深入理解java虚拟机
1.1	方法区：一般存放被虚拟机加载的类信息（类的版本，字段，方法，接口），常量，静态常量，即时编译后的代码信息等。
1.2	堆：一般对象的实例就是在堆上分配内存，它是虚拟机管理的最大的一块内存区域，也是GC最主要的区域。JAVA堆可以处于不连续的内存空间，但是逻辑上是连贯的。
1.3	虚拟机栈：java方法执行的所用到的区域，
1.4	本地方法栈：
1.5	程序计数器：
